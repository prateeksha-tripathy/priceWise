/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['m.media-amazon.com'], // Add your external image host domains here
    },
  };
  
  export default nextConfig;
  